# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "storyboard"

[setup]
script = '''
set -e

cd "$CODEX_WORKTREE_PATH"

SOURCE_ENV_FILE="${STORYBOARD_ENV_LOCAL_PATH:-/Users/user/code/storyboard/.env.local}"
TARGET_ENV_FILE="$CODEX_WORKTREE_PATH/.env.local"

if [ -f "$SOURCE_ENV_FILE" ] && [ "$SOURCE_ENV_FILE" != "$TARGET_ENV_FILE" ]; then
  cp "$SOURCE_ENV_FILE" "$TARGET_ENV_FILE"
fi

pnpm install
'''
